<ruleset>
    <file>/var/www/html/wp-content/plugins/woocommerce-koban-sync</file>
    <exclude-pattern>/var/www/html/wp-content/plugins/woocommerce-koban-sync/src/vendor</exclude-pattern>

    <rule ref="WordPress" />

    <rule ref="WordPress.Files.FileName">
        <!--  Incompatible with composer's autoloader -->
        <exclude-pattern>*.php</exclude-pattern>
    </rule>

    <rule ref="WordPress.Security.EscapeOutput">
        <!-- We don't care about this in the tests directory -->
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>

    <rule ref="Squiz.Commenting.InlineComment.InvalidEndChar">
        <!-- We don't care about this in the test files -->
        <exclude-pattern>**/test-*.php</exclude-pattern>
    </rule>
</ruleset>